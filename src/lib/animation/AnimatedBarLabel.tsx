/**
 * AnimatedBarLabel — alias of `AnimatedLabel` for bar charts (PRC-17
 * slice 3e-v). Kept so existing bar-family imports stay unchanged.
 */
export { AnimatedLabel as AnimatedBarLabel, default } from './AnimatedLabel';
export type { AnimatedLabelProps as AnimatedBarLabelProps } from './AnimatedLabel';
